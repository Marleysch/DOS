Takes in a file with a list with two elements. The first is k, the kth biggest element in the list we want to find.
And l, a list of numbers however long in which we want to find the kth biggest element.
Uses deterministic order selection ~ O(n)